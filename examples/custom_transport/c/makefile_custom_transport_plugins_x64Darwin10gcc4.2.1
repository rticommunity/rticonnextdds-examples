######################################################################
# makefile_dummy_x64Darwin10gcc4.2.1
#
# (c) Copyright, Real-Time Innovations, 2012.  All rights reserved.
# No duplications, whole or partial, manual or electronic, may be made
# without express written permission.  Any such copies, or
# revisions thereof, must display this notice unaltered.
# This code contains trade secrets of Real-Time Innovations, Inc.
#
#
# This makefile was automatically generated by rtiddsgen.
#
# To compile, type:
# 	gmake -f makefile_custom_transport_plugins_x64Darwin10gcc4.2.1
#
# Note: This makefile is only meant to build our example applications and
#       may require alterations to build on your system.
#
# This makefile assumes that your build environment is already correctly
# configured. (For example, the correct version of your compiler and
# linker should be on your PATH.)
######################################################################

c_cc_x64Darwin10gcc4.2.1 = gcc -g
c_cc_flags_x64Darwin10gcc4.2.1 = -m64
c_ld_x64Darwin10gcc4.2.1 = gcc -g
c_ld_flags_x64Darwin10gcc4.2.1 = -m64 -dynamiclib -undefined suppress -flat_namespace
DEFINES_ARCH_SPECIFIC = -DRTI_UNIX -DRTI_DARWIN -DRTI_DARWIN10 -DRTI_64BIT


DEFINES = $(DEFINES_ARCH_SPECIFIC)  

INCLUDES = -I. -I$(NDDSHOME)/include -I$(NDDSHOME)/include/ndds

COMMONSOURCES = FileTransport.c
DYNAMICLIB    = rti_custom_transports
DIRECTORIES   = objs.dir objs/x64Darwin10gcc4.2.1.dir
COMMONOBJS    = $(COMMONSOURCES:%.c=objs/x64Darwin10gcc4.2.1/%.o)

all: x64Darwin10gcc4.2.1

# We actually stick the objects in a sub directory to keep your directory clean.
x64Darwin10gcc4.2.1 : $(DIRECTORIES) $(COMMONOBJS) $(DYNAMICLIB:%=objs/x64Darwin10gcc4.2.1/lib%.dylib)

objs/x64Darwin10gcc4.2.1/%.dylib :$(COMMONOBJS) 
	$(c_ld_x64Darwin10gcc4.2.1) $(c_ld_flags_x64Darwin10gcc4.2.1)  -o $(@) $(COMMONOBJS) 

objs/x64Darwin10gcc4.2.1/%.o : %.c %.h
	$(c_cc_x64Darwin10gcc4.2.1) $(c_cc_flags_x64Darwin10gcc4.2.1)  -o $@ $(DEFINES) $(INCLUDES) -c $<

#
# Uncomment these lines if you want the support files regenerated when idl
# file is modified
#
#dummy.c dummy.h \
#dummyPlugin.c dummyPlugin.h \
#dummySupport.c dummySupport.h : \
#		dummy.idl
#	$(NDDSHOME)/scripts/rtiddsgen dummy.idl -replace -language C

# Here is how we create those subdirectories automatically.
%.dir : 
	@echo "Checking directory $*"
	@if [ ! -d $* ]; then \
		echo "Making directory $*"; \
		mkdir -p $* ; \
	fi;
