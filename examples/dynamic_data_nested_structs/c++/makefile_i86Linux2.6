######################################################################
# makefile_Foo_i86Linux2.6gcc4.1.1
#
# (c) Copyright, Real-Time Innovations, 2012.  All rights reserved.
# No duplications, whole or partial, manual or electronic, may be made
# without express written permission.  Any such copies, or
# revisions thereof, must display this notice unaltered.
# This code contains trade secrets of Real-Time Innovations, Inc.
#
#
# This makefile was automatically generated by rtiddsgen.
#
# To compile, type:
# 	gmake -f makefile_Foo_i86Linux2.6gcc4.1.1
#
# Note: This makefile is only meant to build our example applications and
#       may require alterations to build on your system.
#
# This makefile assumes that your build environment is already correctly
# configured. (For example, the correct version of your compiler and
# linker should be on your PATH.)
######################################################################

c_cc_i86Linux2.6 = gcc
c_cc_flags_i86Linux2.6 = -m32 -Wall
c_ld_i86Linux2.6 = gcc
c_ld_flags_i86Linux2.6 = -m32
cxx_cc_i86Linux2.6 = g++
cxx_cc_flags_i86Linux2.6 = -m32 -Wall
cxx_ld_i86Linux2.6 = g++
cxx_ld_flags_i86Linux2.6 = -m32 -static-libgcc
syslibs_i86Linux2.6 = -ldl -lnsl -lm -lpthread -lrt
DEFINES_ARCH_SPECIFIC = -DRTI_UNIX -DRTI_LINUX

ifndef ARCH
ARCH=i86Linux2.6gcc4.4.5
endif

DEFINES = $(DEFINES_ARCH_SPECIFIC)  

INCLUDES = -I. -I$(NDDSHOME)/include -I$(NDDSHOME)/include/ndds

LIBS = -L$(NDDSHOME)/lib/$(ARCH) \
        -lnddscppz -lnddscz -lnddscorez $(syslibs_i86Linux2.6)

CDRSOURCES    = Foo.idl
COMMONSOURCES = 
EXEC          = dynamic_data_nested_struct_example
DIRECTORIES   = objs.dir objs/$(ARCH).dir
COMMONOBJS    = $(COMMONSOURCES:%.cxx=objs/$(ARCH)/%.o)

# We actually stick the objects in a sub directory to keep your directory clean.
$(ARCH) : $(DIRECTORIES) $(COMMONOBJS) \
	       $(EXEC:%=objs/$(ARCH)/%.o) \
	       $(EXEC:%=objs/$(ARCH)/%.out)

objs/$(ARCH)/%.out : objs/$(ARCH)/%.o 
	$(cxx_ld_i86Linux2.6) $(cxx_ld_flags_i86Linux2.6)  -o $(@:%.out=%) $(@:%.out=%.o) $(COMMONOBJS) $(LIBS)

objs/$(ARCH)/%.o : %.cxx 
	$(cxx_cc_i86Linux2.6) $(cxx_cc_flags_i86Linux2.6)  -o $@ $(DEFINES) $(INCLUDES) -c $<

#
# Uncomment these lines if you want the support files regenerated when idl
# file is modified
#
#Foo.c Foo.h \
#FooPlugin.c FooPlugin.h \
#FooSupport.c FooSupport.h : \
#		Foo.idl
#	$(NDDSHOME)/scripts/rtiddsgen Foo.idl -replace -language C

# Here is how we create those subdirectories automatically.
%.dir : 
	@echo "Checking directory $*"
	@if [ ! -d $* ]; then \
		echo "Making directory $*"; \
		mkdir -p $* ; \
	fi;
